version: "3.9"

services:
  backend:
    build: ./beckend
    container_name: smart-campus-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: smart-campus-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend